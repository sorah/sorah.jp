import { style } from "@vanilla-extract/css";

export const thresholdTable = style({
  fontSize: "1.3em",
  width: "100%",
  maxWidth: "400px",
});

export const numberInput = style({
  fontSize: "24px",
});
